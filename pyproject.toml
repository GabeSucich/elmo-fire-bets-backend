[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "aiosqlite>=0.22.1",
    "alembic>=1.16.5",
    "asyncpg>=0.31.0",
    "bcrypt>=5.0.0",
    "fastapi>=0.128.0",
    "fastapi-login>=1.10.3",
    "greenlet>=3.2.4",
    "openai>=2.48.0",
    "passlib>=1.7.4",
    "pydantic>=2.12.5",
    "pydantic-to-typescript>=2.0.0",
    "python-dotenv>=1.2.1",
    "requests>=2.32.5",
    "sqlalchemy>=2.0.46",
    "sqlmodel>=0.0.31",
    # The IANA zone database. services/odds/client resolves America/Los_Angeles at import,
    # and a slim container image need not ship system tzdata — without this the lookup
    # raises and the whole app fails to boot rather than merely getting a slate wrong.
    "tzdata>=2025.2",
    "uvicorn>=0.39.0",
]
